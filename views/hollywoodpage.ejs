<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MovieShowCase</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Home</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/bollywood">Bollywood</a>
              </li>
            </ul>
          </div>
          <a href="/newMovie" class="btn btn-primary">Add Movie</a>
        </div>
      </nav>
      <% if (movies.length < 1) { %>
        <h1 style="padding: 0px 0px 0px 15px">Oops no added movies</h1>
      <% } else {%>
        <h1 style="padding: 0px 0px 0px 15px">Here are Hollywood Movies</h1>
      <% } %>
      <div class="container">
        <div class="row d-flex flex-wrap">
          <% for (let i = 0; i < movies.length; i++) { %>
            <div class="card m-2" style="width: 23%;">
              <img src="<%= movies[i].image %>" class="card-img-top" alt="Hollywood movie Image" style="height: 200px; object-fit: cover;">
              <div class="card-body">
                <h5 class="card-title"><%= movies[i].name %></h5>
                <p class="card-text"><%= movies[i].name %></p>
                <a href="/hollywood/<%= movies[i]._id%>" class="btn btn-primary">See Details</a>
              </div>
            </div>
          <% } %>
        </div>
      </div>
</body>
</html>